{
  "name": "vk_cms",
  "version": "1.0.0",
  "description": "vk cms system for legenda site",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --env-file=.env index.js",
    "start": "node --env-file=.env index.js"
  },
  "author": "LegedaOS",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
